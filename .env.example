# Copy to .env (never commit it) or export these in your shell.
# Create an API key in the PacketExchange dashboard under API keys.
PACKETEXCHANGE_API_KEY=
# Signing secret shown when you create or rotate a webhook endpoint.
PACKETEXCHANGE_WEBHOOK_SECRET=
